{
  "name": "cstco-inventory-system",
  "version": "1.0.0",
  "description": "CSTCO Inventory Management System",
  "scripts": {
    "dev": "concurrently \"npm --prefix backend start\" \"npm --prefix frontend run dev\"",
    "start": "concurrently \"npm --prefix backend start\" \"npm --prefix frontend run dev\"",
    "backend": "npm --prefix backend start",
    "frontend": "npm --prefix frontend run dev",
    "kill": "taskkill /F /IM node.exe 2>nul || true"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
