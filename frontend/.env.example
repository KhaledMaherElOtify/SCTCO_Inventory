# Frontend environment configuration
# For production builds, the API URL is derived from window.location

# Development environment (for npm run dev)
VITE_API_URL=http://localhost:3001
VITE_API_PORT=3001

# Production: The frontend will automatically use:
# http://<server-ip>:3001
# No need to configure the API URL in production
